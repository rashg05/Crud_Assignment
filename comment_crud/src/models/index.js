const user = require('../../../yap_assignment');
const posts = require('../../../post_yap_assignment');
const comments = require('./comments.model');

module.exports = {
  user,
  posts,
  comments,
};